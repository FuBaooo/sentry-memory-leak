{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "build:preview": "nitro build && pnpm node:inspect",
    "node:inspect": "node --inspect .output/server/index.mjs",
    "test": "bash ./test.sh"
  },
  "devDependencies": {
    "nitropack": "latest"
  },
  "dependencies": {
    "@sentry/node": "^7.113.0"
  }
}
